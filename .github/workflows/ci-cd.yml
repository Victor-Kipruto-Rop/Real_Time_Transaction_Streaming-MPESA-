name: CI/CD Pipeline - M-Pesa Streaming

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/mpesa-transaction-streaming

jobs:
  # ===================================================================
  # Code Quality & Linting
  # ===================================================================
  lint:
    runs-on: ubuntu-latest
    name: Lint & Format Check
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
      
      - name: Run Black (code formatting)
        run: black --check ingestion/ streaming/ schemas/ tests/
      
      - name: Run Flake8 (linting)
        continue-on-error: true
        run: flake8 ingestion/ streaming/ schemas/ tests/ --max-line-length=100 --count --statistics --extend-ignore=E228,E722,F401,F541,F811,F841
      
      - name: Run isort (import sorting)
        continue-on-error: true
        run: isort --check-only ingestion/ streaming/ schemas/ tests/

  # ===================================================================
  # Type Checking
  # ===================================================================
  type-check:
    runs-on: ubuntu-latest
    name: Type Checking (mypy)
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      
      - name: Run mypy type checker
        run: mypy ingestion/ streaming/ schemas/ --ignore-missing-imports --no-error-summary || true

  # ===================================================================
  # Unit Tests
  # ===================================================================
  test:
    runs-on: ubuntu-latest
    name: Unit Tests
    continue-on-error: true
    
    services:
      postgres:
        image: postgres:15-alpine
        env:
          POSTGRES_DB: mpesa_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432
      
      kafka:
        image: confluentinc/cp-kafka:7.5.0
        env:
          KAFKA_BROKER_ID: 1
          KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
          KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://kafka:9092
          KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
          cache: 'pip'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt
          pip install -r requirements.txt
      
      - name: Create .env file for tests
        run: |
          cat > .env << EOF
          POSTGRES_HOST=localhost
          POSTGRES_PORT=5432
          POSTGRES_DB=mpesa_test
          POSTGRES_USER=test_user
          POSTGRES_PASSWORD=test_password
          KAFKA_BROKERS=localhost:9092
          KAFKA_TOPIC_TRANSACTIONS=mpesa-transactions
          LOG_LEVEL=INFO
          EOF
      
      - name: Setup PostgreSQL test database
        run: |
          echo "PostgreSQL test database ready (JSON support is built-in)"
      
      - name: Run pytest with coverage
        run: |
          pytest tests/ -v --cov=ingestion --cov=streaming --cov=schemas --cov-report=xml --cov-report=term-missing
        env:
          POSTGRES_HOST: localhost
          POSTGRES_PORT: 5432
          POSTGRES_DB: mpesa_test
          POSTGRES_USER: test_user
          POSTGRES_PASSWORD: test_password
          KAFKA_BROKERS: localhost:9092
      
      - name: Upload coverage reports
        uses: codecov/codecov-action@v3
        with:
          files: ./coverage.xml
          flags: unittests
          name: codecov-umbrella

  # ===================================================================
  # Security Scanning
  # ===================================================================
  security:
    runs-on: ubuntu-latest
    name: Security Scanning
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.12'
      
      - name: Install bandit
        run: pip install bandit[toml]
      
      - name: Run Bandit security check
        run: bandit -r ingestion/ streaming/ schemas/ -f json -o bandit-report.json || true
      
      - name: Check for secrets (truffleHog)
        if: github.event_name == 'pull_request'
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: ${{ github.event.pull_request.base.sha }}
          head: ${{ github.event.pull_request.head.sha }}
      
      - name: Scan filesystem for secrets (push events)
        if: github.event_name == 'push'
        run: |
          docker run --rm -v "$PWD:/scan" trufflesecurity/trufflehog:latest filesystem /scan --only-verified --json > trufflehog-results.json || true
          if [ -s trufflehog-results.json ]; then
            echo "⚠️ Potential secrets found. Review trufflehog-results.json"
            cat trufflehog-results.json
          fi

  # ===================================================================
  # Docker Build & Push
  # ===================================================================
  build:
    needs: [lint, type-check, test]
    runs-on: ubuntu-latest
    name: Build Docker Image
    continue-on-error: true
    permissions:
      contents: read
      packages: write
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2
      
      - name: Log in to Container Registry
        uses: docker/login-action@v2
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v4
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha
      
      - name: Build and push Docker image
        uses: docker/build-push-action@v4
        with:
          context: .
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache
          cache-to: type=registry,ref=${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:buildcache,mode=max

  # ===================================================================
  # Deployment (Optional - for main branch)
  # ===================================================================
  deploy:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    needs: build
    runs-on: ubuntu-latest
    name: Deploy to Staging
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Deploy to staging environment
        run: |
          echo "Deploying to staging..."
          # Add your deployment commands here
          # Example: kubectl apply -f k8s/
        env:
          KUBE_CONFIG: ${{ secrets.KUBE_CONFIG }}

  # ===================================================================
  # Notification (Optional - requires SLACK_WEBHOOK_URL secret)
  # ===================================================================
  notify:
    if: always() && vars.ENABLE_SLACK_NOTIFICATIONS == 'true'
    needs: [lint, type-check, test]
    runs-on: ubuntu-latest
    name: Notify Slack
    continue-on-error: true
    
    steps:
      - name: Slack notification
        continue-on-error: true
        uses: slackapi/slack-github-action@v2
        with:
          payload: |
            {
              "text": "CI/CD Pipeline for M-Pesa Streaming",
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*Pipeline Status*: ${{ job.status }}\n*Branch*: ${{ github.ref }}\n*Commit*: ${{ github.sha }}"
                  }
                }
              ]
            }
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
